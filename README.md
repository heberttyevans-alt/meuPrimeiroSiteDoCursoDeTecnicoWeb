# Primeiro Site em HTML e CSS – Estudo Guiado

Este repositório contém o **primeiro projeto de site em HTML e CSS** desenvolvido como estudo guiado na disciplina **Programação para Web I (PPW1)** do curso Técnico em Informática para Internet – IF.

A ideia é que você acompanhe o passo a passo, entenda cada parte do código e use este projeto como base para criar seus próprios sites.

---

## 🎯 Objetivo do Projeto

- Apresentar, na prática, como montar uma **página web completa** usando apenas **HTML e CSS**.
- Treinar **estrutura semântica** com tags modernas do HTML5.
- Praticar **layout com Flexbox** (menus, colunas, cards, etc.).
- Mostrar como deixar o site com cara de página real (hero, cards de destaque, seção sobre, etc.).

---

## 🧱 O que você vai encontrar neste projeto

Ao explorar o código, você verá exemplos práticos de:

### Estrutura HTML

- `<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`
- Tags semânticas: `<header>`, `<nav>`, `<main>`, `<section>`
- Conteúdo: `<h1>`, `<h2>`, `<p>`, `<a>`, `<ul>`, `<li>`, `<img>`, `<strong>`

### Layout e Estilo (CSS)

- Menu de navegação horizontal
- Seção **Hero** com imagem de fundo e texto centralizado
- Seção de **cards de destaque** (“Features”) em 3 colunas
- Seção **Sobre o Curso**
- Seção **Sobre Nós** com **imagem de um lado e texto do outro** (layout de 2 colunas)
- Conceitos de CSS:
  - `display: flex`, `justify-content`, `align-items`, `flex: 1`
  - `padding`, `margin`, `border-radius`, `box-shadow`
  - `background-image`, `background-size: cover`
  - Ajustes de tipografia (`font-size`, `line-height`, etc.)

---

## 📂 Estrutura do Repositório

A estrutura básica do projeto é:

```bash
/
├── index.html      # Página principal do site
├── estilos.css     # Arquivo de estilos (CSS)
└── img/         # Imagens usadas no site (ex: dev.jpg, imagem-sobre.jpg)
